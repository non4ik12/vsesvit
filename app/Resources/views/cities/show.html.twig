{% extends 'base.html.twig' %}
{% block body %}
    <div class="vs-page-title">
		<h1>{{ city.title }}</h1>
	</div>
	<div class="row-fluid vs-page-content">
		{% autoescape %}
			{{ city.description|raw }} {# var won't be escaped #}
		{% endautoescape %}
	</div>

{% endblock %}